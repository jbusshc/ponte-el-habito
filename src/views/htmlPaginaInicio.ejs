<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= titulo %></title>
    <link rel="stylesheet" href="/css/paginaInicioStyle.css">
</head>
<body>
    <%- include('partial/_header') %>  <!-- Incluir el header -->
    
    <main>
        <h2>¿Qué hábito cumpliremos hoy?</h2>

        <!-- Tabla para mostrar los hábitos -->
        <table>
            <thead>
                <tr>
                    <th>ID</th>
                    <th>Nombre del Hábito</th>
                    <th>Tipo de Hábito</th>
                </tr>
            </thead>
            <tbody>
                <% habitos.forEach(habito => { %>
                    <%
                        let tipo_habito = '';
                        if (habito.HABITO_IDT == 1) {
                            tipo_habito = 'Cuantitativo';
                        } else {
                            tipo_habito = 'Binario';
                        }
                    %>
                    <tr>
                        <td><%= habito.HABITO_ID %></td>
                        <td><%= habito.HABITO_NOM %></td>
                        <td><%= tipo_habito %></td>
                    </tr>
                <% }) %>
            </tbody>
        </table>
    </main>
    
</body>


</html>
